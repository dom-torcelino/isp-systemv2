/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you used absolute imports/paths in Vite via tsconfig paths, enable below:
  // experimental: { typedRoutes: true },
  // images: { unoptimized: true }, // uncomment if you had raw images without domain config
};

export default nextConfig;
